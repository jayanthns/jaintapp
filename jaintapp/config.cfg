[db_pgsql]
ENGINE = django.db.backends.postgresql_psycopg2
NAME = jaintdb  // Your db name
USER = jaint  // username of the db
PASSWORD = password  // password of the db
HOST = localhost  // HOST URL
PORT = 5432  // PORT of the url